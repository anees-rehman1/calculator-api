name: Daily Health Check

# Run every day at 8 AM UTC
on:
  schedule:
    - cron: '0 8 * * *'
  # Also can run manually from GitHub UI
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Health Check
      run: |
        echo "📊 Project Health Check"
        echo "✅ Dependencies: $(npm list --depth=0 | wc -l) packages"
        echo "✅ Test files: $(find tests -name '*.test.js' | wc -l) tests"
        echo "✅ Last commit: $(git log -1 --format=%cr)"
